{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The interface is served by openwork-server, so IPC arrives from a remote origin and has to be granted explicitly. The runtime manager picks its own port, so the grant covers loopback rather than one number.",
  "windows": [
    "main"
  ],
  "remote": {
    "urls": [
      "http://127.0.0.1:*",
      "http://localhost:*"
    ]
  },
  "permissions": [
    "core:default",
    "dialog:default",
    "allow-desktop-native"
  ]
}
