{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The spike loads the interface from openwork-server, so IPC has to be granted to that remote origin explicitly.",
  "windows": [
    "main"
  ],
  "remote": {
    "urls": [
      "http://127.0.0.1:8799"
    ]
  },
  "permissions": [
    "core:default",
    "dialog:default",
    "allow-desktop-native"
  ]
}
