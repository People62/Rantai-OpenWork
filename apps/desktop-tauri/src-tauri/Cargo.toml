[package]
name = "rantai-tauri-spike"
version = "0.0.0"
edition = "2021"
description = "Stage 1 spike: does the Rantai interface render under the system webview?"
publish = false

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.dev]
incremental = true
